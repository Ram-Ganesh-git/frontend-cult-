//variable. (variable can/hold store the data.)

/*
  variable can store the data.
  3 Types of variable in Js
   1) Var. (variable)
   2) Let. (let,)
   3) Const. (consist,)

   Declaring a variable 
   A bottle (var,let,const) can store the water (value)
   My Firstname(var,let,const) is Ram. (value)
   My Lastname (var,let,const) is valukuru.(value)
   = (Assigning the value)
   // Re-declaring the value

*/

let firstName = 'Ram'; //Declaring a variable
const lastName = 'valukuru';
let age = 30;

// we can re-assign 'let' vars. but we cant re-assign 'const' variable.  
firstName = 'Ram 2';
//lastName = 'rrttgkfjfjf';


console.log(firstName);
console.log(lastName);
console.log(age);

// Rules for Declaring var
// OnlyLetters, numbers, underscore and Dollier sym
// - Dont start with a number

// with let we can declare let without value - later we can assign the value.
let score;
score = 100;
score = 500;

console.log(score);

// Declare Multiple values at a time.
let a, b, c;
const d = 10, e = 20, f = 30;

console.log(d);
console.log(a);


